import { Input } from "@chakra-ui/react";

function Search(props) {
  return (
    <div>
      <Input w="600px" placeholder="Find Block, Transactions, Address" />
    </div>
  );
}

export default Search;
